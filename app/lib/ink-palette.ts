// INK'S ONE PALETTE — Sam, 2026-09-23: "some pages are blue and others green".
//
// Every data mark on every ink screen — chart bars, the carrier bars, the
// delivery-window bar, each open's point and line on the map — is ONE blue:
// Polaris Viz's own single-series colour in its Light theme (polaris-viz-core
// constants LIGHT_BLUE_10_SOLID), so the charts and the hand-drawn marks are
// the same family. Everything else is Polaris neutral. Green is reserved for
// one thing: the "Seen at the door" badge (Polaris Badge tone="success").

export const INK_DATA = "#13ACF0";
export const INK_DATA_TINT = "rgba(19, 172, 240, 0.14)";
export const INK_NEUTRAL = "#303030";
export const INK_MUTED = "#8a8a8a";
export const INK_HAIRLINE = "#d4d4d4";
