# HELD FOR SAM — The Ritualist ships no order-status block: it never rendered, and App Store review paused on it (5.6.1)

## Current checkpoint

- Task: task-852a6af2-6c3c-476e-b897-89b67a9755f1
- Status: working
- Branch: review/the-order-status-block-comes-out
- PR: 160
- Assigned agent: claude
- Current session: 3b5f52c4-b451-4ee7-bfac-405da269c474
- Updated: 2026-09-24T16:57:53.965Z

## Request

Track PR #160: Shopify paused The Ritualist's review on 5.6.1 because its order-status customer-account block shows nothing. The PR deletes extensions/order-page-block, the Settings › Communication 'The order status page' section, its API route and the ink:order_door metafield writer. HELD FOR SAM: merge, then his `shopify app deploy --config shopify.app.toml` from clean main, then Submit fixes in the Partner Dashboard.

## Published code

6ba1ee20223394397c0ec479ae329447de381909

## Summary

Shopify paused The Ritualist (ref 104187) on 5.6.1: its order-status customer-account block shows nothing. Measured: it never rendered (08-23); Help Center says blocks must be placed in the checkout & accounts editor; its ink:order_door metafield needs a brand_slug that neither the review store nor Corvara has; the ink namespace is hidden from customer-account extensions. PR #160 deletes extensions/order-page-block, the Settings › Communication 'The order status page' section, app.api.settings.order-door, order-door-metafield.server(+test) and both webhook calls. Live config == main toml (config pull diff), so the deploy only removes the extension, with no re-consent.

## Decisions

- Remove the block rather than fix it: it never rendered, cannot self-place, and has nothing to show on a store without a brand page
- Deleted the metafield writer too (restore path = revert #160); the metafields already written are inert

## Checks actually run

- npm run typecheck: 0 errors
- npx vitest run: 659 passed, 1 todo
- npm run build: green
- CI check / container-node / merge-gate: pass
- Settings › Communication rendered before/after in the local harness

## Next action

HELD FOR SAM: merge #160 → `shopify app deploy --config shopify.app.toml` from clean main (CLI must list only 'ink order page' removed) → Partner Dashboard Submit fixes (Run automated checks first if expired). #106 is moot while the block is out.

Checkpoint writer: claude; 2026-09-24T16:46:56.054Z

Older checkpoints and decisions remain in this file's Git history.
